import React, { Component } from 'react';
//This page can be the navigation we want on all pages or can be changed to home, right now I was just testing it - Abby
class Navigation extends Component {
    render() {
        return (
            <div>
                <h1> Welcome </h1>
            </div>
        )
    }
}

export default Navigation;